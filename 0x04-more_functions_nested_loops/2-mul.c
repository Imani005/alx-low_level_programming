#include "main.h"

/**
 * mul - Multiplies two intergers
 * @x: The first number to multiply
 * @y: The second number to multiply with the first number
 * Return: The result of multiplication between the x param and the y param
 */

int mul(int x, int y)
{
	return (x * y);
}
